{% extends 'base.html.twig' %}

{% block title %}{{ 'empresa.afegir'|trans }}{% endblock %}
{% block title_block %}
  {{ 'empresa.afegir'|trans }}
{% endblock %}

{% block body %}
<div class="container mt-4">
		<div class="pt-2">
			

			{{ form_start(form) }}
				{{ form_rest(form) }}


             
				<div class="w-100 text-center">
					<button type="submit" class="btn btn-outline-primary"><i class="fa fa-paper-plane"></i> Enviar</button>
				</div>

			{{ form_end(form) }}
		</div>

	</div>
{% endblock %}
