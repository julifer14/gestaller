{% extends 'base.html.twig' %}

{% block title %}
  {{ 'factura.add'|trans }}
{% endblock %}
{% block title_block %}
  {{ 'factura.add'|trans }}
{% endblock %}

{% block body %}
  <div class="container mt-4">
    <div class="pt-2">
      {{ form_start(form) }}
      <div class="row">
        <div class="col">
          {{ form_row(form.any) }}
        </div>
        <div class="col">
          {{ form_row(form.data) }}
        </div>
      </div>
      {{ form_row(form.tasca) }}
      <div class="row">
        <div class="col">
          {{ form_row(form.quilometres) }}
        </div>
        <div class="col">
          {{ form_row(form.iva) }}
        </div>
      </div>
      <div class="row">
        <div class="col">
          {{ form_row(form.vehicle) }}
        </div>
        <div class="col">
          {{ form_row(form.treballador) }}
        </div>
      </div>
      <div class="row">
        <div class="col">
          {{ form_row(form.estat) }}
        </div>
        <div class="col">
          {{ form_row(form.formaPagament) }}
        </div>
      </div>
      <div class="row">
        <div class="col">
          {{ form_row(form.observacions) }}
        </div>
      </div>

      <div clas="row">
        {{ form_row(form.liniaFacturas) }}
        <button class="btn btn-primary add_linia_fac"
          data-id-factura="{{ form.vars.data.id }}">
          Afegir linia
        </button>
      </div>
      <div id="linias"></div>

     <!-- {{ form_rest(form) }}-->

      <div class="w-100 text-center">
        <button type="submit" class="btn btn-outline-primary">
          <i class="fa fa-paper-plane"></i>Guardar
        </button>
        <!--<a href="/factures/?pagar" class="btn btn-outline-secondary">Pagar</a>-->
        <!--<button type="submit" id="pagarFactura" data-id-factura="{{ form.vars.data.id }}" class="btn btn-outline-secondary">
          <i class="fa fa-paper-plane"></i>Pagar
        </button>-->

        
      </div>

      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
